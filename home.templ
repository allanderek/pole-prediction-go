package main

templ Footer() {
	<footer class="bg-slate-900"></footer>
}

templ HomePage(cookieInfo CookieInfo) {
	@Page(
		"Pole prediction",
		"Predicting grand prix since, sometime this century",
		"/",
		cookieInfo,
	) {
		<div class="bg-white">
			@Footer()
		</div>
	}
}
